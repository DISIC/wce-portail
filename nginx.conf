server {
    listen 80;	
    sendfile on;
    default_type application/octet-stream; 
    # set_real_ip_from 10.167.0.0/16;
    # server_name preprod.webconf.numerique.gouv.fr;

	root /usr/share/nginx/html;

    ### VOXAPI ################################################
    ### VOXAPI THROUGH PROXY
    # location ^~ /voxapi/ {
    #     proxy_set_header X-Real-IP  $remote_addr;
    #     proxy_set_header Host-Real-IP  $http_host;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Real-Pcol http;

    #     proxy_intercept_errors on;
    #     proxy_set_header Host api.voxify.preprod.joona.fr:443;
    #     proxy_set_header Authorization "Api-Key ZrkHinmA";
    # proxy_hide_header Access-Control-Allow-Origin;
    # proxy_pass https://127.0.0.1:8443/;
    # }

    ### EXCALIDRAW ################################################
    ### EXCALIDRAW ROOT REDIRECTION
    # location = /excalidraw {
    #     return 301 https://$host/wce-api/;
    # }

	### EXCALIDRAW WHITEBOARD BACKEND
    # location ^~ /whiteboard/ {
    #     proxy_set_header X-Real-IP  $remote_addr;
    #     proxy_set_header Host-Real-IP  $http_host;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_pass http://127.0.0.1:3001/;
    # }

	# location ^~ /socket.io {
    # 		proxy_pass http://127.0.0.1:3001/socket.io/?$args;
    # 		proxy_http_version 1.1;
    # 		proxy_set_header Upgrade $http_upgrade;
    # 		proxy_set_header Connection "upgrade";
    # 		proxy_set_header Host $http_host;
    # 		tcp_nodelay on;
	# }

    ### JIGASI ################################################
	### FOR FREESWITCH
	# location ~* ^/JigasiInstanceHook {
	#     default_type application/json;
	#     return 200 '{"instance": "$jigasi_server"}';
	# }
	
	### WCE-PORTAIL-REACT #####################################
	### Ensure all static content can always be found first
	location ~ ^/(static/(css|js|media)|images|stylesheets)/(.*)$ {
    	    add_header Access-Control-Allow-Origin *;
            add_header Cache-Control "public, max-age=31536000, immutable";
    	    # alias /opt/wce-portail/$uri;

    	# cache all versioned files
	    if ($arg_v) {
        	expires 1y;
    	}
	}

	location ~ ^/(.*)$ {
    	    try_files $uri @root_path;
	}

	location @root_path {
   	    rewrite ^/(.*)$ / break;
	}

    # Rediriger les URLs "random" vers Jitsi
    location ~ "^/[a-zA-Z0-9_-]{10,}" {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    	# Anything that didn't match above, and isn't a real file, assume it's a room name and redirect to /
#    	location ~ ^/([^/?&:'"]+)/(.*)$ {
#            rewrite ^/([^/?&:'"]+)/(.*)$ /$2;
#    	}
}
